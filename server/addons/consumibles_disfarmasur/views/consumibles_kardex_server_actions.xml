<odoo>

    <!-- ACCIÓN SERVER: EXPORTAR KARDEX PAGINA 1 -->
    <record id="action_export_kardex_page_1_excel" model="ir.actions.server">
        <field name="name">📥 Exportar Kardex (Página 1)</field>
        <field name="model_id" ref="model_consumibles_product_kardex"/>
        <field name="binding_model_id" ref="model_consumibles_product_kardex"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
action = records.action_export_kardex_page_1_excel()
        </field>
    </record>

</odoo>
