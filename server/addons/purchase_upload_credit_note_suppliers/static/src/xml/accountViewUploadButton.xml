<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="purchase_upload_credit_note_suppliers.ListView.Buttons"
       t-inherit="account.ListView.Buttons"
       t-inherit-mode="extension"
       owl="1">
        <xpath expr="//div[hasclass('o_list_buttons')]" position="inside">
            <!-- Mostrar solo en Notas de Crédito de Proveedores -->
            <t t-if="props?.context?.default_move_type === 'in_refund'">
                <button type="button"
                        t-on-click="openCreditNoteUploadWizard"
                        class="btn btn-secondary o_button_upload_bill">
                    Subir .txt
                </button>
            </t>
        </xpath>
    </t>
</templates>